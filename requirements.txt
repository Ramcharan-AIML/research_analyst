# ============================================================
# Multi-Agent Research System — requirements.txt
# Stack: LangChain (LCEL / Runnables) + LangGraph + Mistral
#        + Tavily search tool + BeautifulSoup scraping tool
# Tested against the LangChain 1.x ecosystem (current as of Aug 2026)
# ============================================================

# --- Core LangChain framework -------------------------------
# langchain 1.x is the current major line (Oct 2025 onward).
# It ships LCEL/Runnables, the new create_agent() agent builder,
# and pulls in langgraph + langchain-core automatically.
langchain>=1.3.0,<2.0.0
langchain-core>=1.4.0,<2.0.0

# LangGraph — the runtime create_agent()/create_react_agent() run on.
# Also needed directly if you build your own multi-agent graph
# (e.g. a supervisor node routing between your two agents).
langgraph>=1.0.0,<2.0.0

# Community integrations (kept for any loaders/utilities you pull
# in beyond the two tools below).
langchain-community>=0.4.0,<0.5.0

# --- Mistral LLM integration ---------------------------------
langchain-mistralai>=0.2.10

# --- Tool 1: Tavily web search ---------------------------------
# langchain-tavily is the official, currently-maintained integration
# (the old TavilySearchResults inside langchain-community is deprecated).
langchain-tavily>=0.2.8
tavily-python>=0.5.0

# --- Tool 2: BeautifulSoup web scraping -------------------------
beautifulsoup4>=4.12.3
lxml>=5.3.0          # fast parser backend for BeautifulSoup
requests>=2.32.3     # fetch raw HTML before parsing with bs4

# --- Config / environment -------------------------------------
python-dotenv>=1.0.1   # load MISTRAL_API_KEY / TAVILY_API_KEY from .env
pydantic>=2.8.0        # structured tool inputs/outputs, schema validation

# --- Optional: only needed if you also want legacy-style agents -
# (string-based ReAct prompts pulled from LangChain Hub, AgentExecutor,
# or old-style Chains). Not required for the LCEL/Runnables + create_agent
# approach described above — uncomment only if you need them.
# langchain-classic>=1.0.0
# langchainhub>=0.1.21
